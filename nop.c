#include "monty.h"

/**
 * nop_func - No operation, does nothing.
 * @stack: Pointer to the stack.
 * @h: Line number in the Monty file.
 *
 * Return: No return value.
 */
void nop_func(stack_t **stack, unsigned int h)
{
    (void)stack;
    (void)h;
}

